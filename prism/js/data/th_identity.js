/* global angular, _, Backbone, PouchDB, $, emit */

// communication layer using telehash

(function () {
    angular.module('dtouprism')
        .factory('th_identity', function(utils) {

        });
})();
